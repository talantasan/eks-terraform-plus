mariadb:
  enabled: false
externalDatabase:
  host: ${rds_endpoint}
externalDatabase:
  user: ${myuser}
externalDatabase:
  password: ${mypassword}
externalDatabase:
  database: ${mydatabase}
externalDatabase:
  port: 3306
ingress:
  enabled: true
  hostname: {var_ingress_hostname}


